spring:
  application:
    name: spring-ai-mcp-client
  profiles:
    active: test
  ai:
    openai:
      api-key: sk-xxx                   # 官方 deepseek
      base-url: https://api.deepseek.com
      chat:
        options:
          model: deepseek-chat

logging:
  level:
    root: info